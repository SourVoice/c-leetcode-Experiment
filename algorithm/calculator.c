#include "sqstack.h"
#include <stdio.h>

int main()
{
    sqstack *number;   //ÔËËãÊıÕ»
    sqstack *operator; //ÔËËã·ûÕ»
    char equation[MAXSIZE];
    while (equation[i++] = getchar())
    {
        if ()
    }
}